import React, { Fragment } from 'react';
import PropTypes from 'prop-types';

const CheckingAccount = props => {
	return <Fragment>Checking Account</Fragment>;
};

CheckingAccount.propTypes = {};

export default CheckingAccount;
